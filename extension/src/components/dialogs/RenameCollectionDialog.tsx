import type { Collection } from "../../types";
export default function RenameCollectionDialog(_: { open: boolean; collection: Collection; onOpenChange: (o: boolean) => void; onRenamed: (c: Collection) => void }) {
  return null;
}
