import type { Collection } from "../../types";
export default function DeleteCollectionDialog(_: { open: boolean; collection: Collection; onOpenChange: (o: boolean) => void; onDeleted: (id: string) => void }) {
  return null;
}
