import type { Collection } from "../types";
export default function CollectionList(_: { collections: Collection[]; searchQuery: string; onSelect: (c: Collection) => void; onRename: (c: Collection) => void; onDelete: (c: Collection) => void }) {
  return null;
}
